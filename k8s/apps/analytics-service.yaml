apiVersion: apps/v1
kind: Deployment
metadata:
  name: analytics-service
  namespace: agrosolutions-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: analytics-service
  template:
    metadata:
      labels:
        app: analytics-service
    spec:
      containers:
        - name: analytics-service
          image: ghcr.io/placeholder/agrosolutions-analytics-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
              name: http
          envFrom:
            - configMapRef:
                name: app-config
          env:
            - name: ConnectionStrings__DefaultConnection
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: postgres-analytics-conn
            - name: ConnectionStrings__MongoDb
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: mongodb-conn
            - name: JwtSettings__SecretKey
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: jwt-secret-key
            - name: JwtSettings__Issuer
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: jwt-issuer
            - name: JwtSettings__Audience
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: jwt-audience
            - name: JwtSettings__ExpirationMinutes
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: jwt-expiration-minutes
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 15
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 30
            periodSeconds: 15
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: analytics-service
  namespace: agrosolutions-app
spec:
  selector:
    app: analytics-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080

